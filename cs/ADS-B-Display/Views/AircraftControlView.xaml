<UserControl
    x:Class="ADS_B_Display.Views.AircraftControlView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:ADS_B_Display.Views.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:local="clr-namespace:ADS_B_Display.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:usercontrols="clr-namespace:ADS_B_Display.Views.UserControls"
    d:DataContext="{d:DesignInstance Type=local:AircraftControlViewModel}"
    d:DesignHeight="1200"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:SecToDateTime x:Key="secToTime" />
        <!--  IsTextConverter 등록  -->
        <converters:IsTextConverter x:Key="IsTextConverter" />
        <!--  HeaderTemplate 정의  -->
        <DataTemplate x:Key="GroupBoxHeaderTemplate">
            <ContentPresenter Content="{Binding}">
                <ContentPresenter.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="FontSize" Value="12" />
                        <Setter Property="FontWeight" Value="Bold" />
                    </Style>
                </ContentPresenter.Resources>
            </ContentPresenter>
        </DataTemplate>
        <Style TargetType="GroupBox">
            <Setter Property="HeaderTemplate" Value="{StaticResource GroupBoxHeaderTemplate}" />
            <Setter Property="Background" Value="{DynamicResource Aurora.Brushes.GroupBox.GroupBox21}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="BorderBrush" Value="#141516" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Border
                                Grid.ColumnSpan="2"
                                Margin="0"
                                Padding="15"
                                Background="#d0d0d0"
                                CornerRadius="4">
                                <Border.Effect>
                                    <DropShadowEffect
                                        BlurRadius="5"
                                        Direction="320"
                                        RenderingBias="Quality"
                                        ShadowDepth="8"
                                        Color="#d0d0d0" />
                                </Border.Effect>
                            </Border>
                            <Border
                                Grid.ColumnSpan="2"
                                Margin="0"
                                Padding="1"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border
                                        x:Name="HeaderContent"
                                        MinHeight="30"
                                        Margin="0"
                                        Padding="11,0"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter VerticalAlignment="Center" ContentSource="Header" />
                                    </Border>
                                    <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}" />
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Header" Value="{x:Null}">
                                <Setter TargetName="HeaderContent" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="Header" Value="">
                                <Setter TargetName="HeaderContent" Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <DataTrigger Binding="{Binding Header, RelativeSource={RelativeSource Self}, Converter={StaticResource IsTextConverter}}" Value="False">
                                <Setter TargetName="HeaderContent" Property="Height" Value="Auto" />
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <ScrollViewer
        Grid.Column="1"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
        VerticalScrollBarVisibility="Auto">

        <StackPanel Margin="8,0,8,8">

            <!--  ─── 1) System Info ───  -->
            <GroupBox Padding="10">
                <StackPanel>
                    <Grid>
                        <TextBlock
                            Margin="0,0,0,0"
                            HorizontalAlignment="Left"
                            FontWeight="Bold"
                            Text="System Time:" />
                        <TextBlock
                            Margin="0"
                            HorizontalAlignment="Right"
                            Text="{Binding SystemTime, FallbackValue=21:24:17:362}" />
                    </Grid>
                    <Grid>
                        <TextBlock
                            Margin="0,0,0,0"
                            HorizontalAlignment="Left"
                            FontWeight="Bold"
                            Text="Mouse Lat:" />
                        <TextBlock
                            Margin="0"
                            HorizontalAlignment="Right"
                            Text="{Binding LatitudeOfMouse, FallbackValue='123 A'}" />
                    </Grid>
                    <Grid>
                        <TextBlock
                            Margin="0,0,0,0"
                            HorizontalAlignment="Left"
                            FontWeight="Bold"
                            Text="Mouse Lon :" />
                        <TextBlock
                            Margin="0"
                            HorizontalAlignment="Right"
                            Text="{Binding LongitudeOfMouse, FallbackValue='123 A'}" />
                    </Grid>

                    <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,0,0"
                            FontWeight="Bold"
                            Text="Num of Aircraft:" />
                        <TextBlock Margin="4,0,0,0" Text="66" />
                        <TextBlock
                            Margin="16,0,0,0"
                            FontWeight="Bold"
                            Text="  Viewable:" />
                        <TextBlock Margin="4,0,0,0" Text="33" />
                    </StackPanel>
                </StackPanel>
            </GroupBox>


            <GroupBox Margin="0,10,0,0" Header="Close Control">
                <Grid>
                    <Grid.Resources>
                        <Style x:Key="TitleText" TargetType="TextBlock">
                            <Setter Property="Margin" Value="0,4,0,0" />
                        </Style>
                        <Style x:Key="ValueText" TargetType="TextBlock">
                            <Setter Property="Margin" Value="8,0,0,0" />
                        </Style>
                    </Grid.Resources>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="0"
                        FontWeight="Bold"
                        Style="{StaticResource TitleText}"
                        Text="ICAO:" />
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="0"
                        Style="{StaticResource ValueText}"
                        Text="{Binding Aircraft.ICAO, FallbackValue=A1E946}" />
                    <TextBlock
                        Grid.Row="0"
                        Grid.Column="1"
                        FontWeight="Bold"
                        Style="{StaticResource TitleText}"
                        Text="FLIGHT #:" />
                    <TextBlock
                        Grid.Row="1"
                        Grid.Column="1"
                        Style="{StaticResource ValueText}"
                        Text="{Binding Aircraft.FlightNum, FallbackValue=RPA5678}" />
                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="0"
                        FontWeight="Bold"
                        Style="{StaticResource TitleText}"
                        Text="LAT:" />
                    <TextBlock
                        Grid.Row="3"
                        Grid.Column="0"
                        Style="{StaticResource ValueText}"
                        Text="{Binding Aircraft.Latitude, FallbackValue=40°38`07 N}" />
                    <!--  40°38'07.13&quot; N  -->
                    <TextBlock
                        Grid.Row="2"
                        Grid.Column="1"
                        FontWeight="Bold"
                        Style="{StaticResource TitleText}"
                        Text="LON:" />
                    <TextBlock
                        Grid.Row="3"
                        Grid.Column="1"
                        Style="{StaticResource ValueText}"
                        Text="{Binding Aircraft.Longitude, FallbackValue=80°01`07.02 W}" />
                    <TextBlock
                        Grid.Row="4"
                        Grid.ColumnSpan="2"
                        FontWeight="Bold"
                        Style="{StaticResource TitleText}"
                        Text="SPD:" />
                    <TextBlock
                        Grid.Row="5"
                        Grid.ColumnSpan="2"
                        Style="{StaticResource ValueText}"
                        Text="{Binding Aircraft.Speed, FallbackValue=273.00 KTS  VRATE:-64.00}" />
                    <TextBlock
                        Grid.Row="6"
                        Grid.Column="0"
                        FontWeight="Bold"
                        Style="{StaticResource TitleText}"
                        Text="HDG:" />
                    <TextBlock
                        Grid.Row="7"
                        Grid.Column="0"
                        Style="{StaticResource ValueText}"
                        Text="{Binding Aircraft.Speed, FallbackValue=227.00 DEG}" />
                    <TextBlock
                        Grid.Row="6"
                        Grid.Column="1"
                        FontWeight="Bold"
                        Style="{StaticResource TitleText}"
                        Text="ALT:" />
                    <TextBlock
                        Grid.Row="7"
                        Grid.Column="1"
                        Style="{StaticResource ValueText}"
                        Text="{Binding Aircraft.Speed, FallbackValue=5800.00 FT}" />
                    <TextBlock
                        Grid.Row="8"
                        Grid.ColumnSpan="2"
                        FontWeight="Bold"
                        Style="{StaticResource TitleText}"
                        Text="MSG CNT :" />
                    <StackPanel Grid.Row="9" Orientation="Horizontal">
                        <TextBlock Style="{StaticResource ValueText}" Text="Raw : " />
                        <TextBlock Text="{Binding Aircraft.NumMessagesRaw, FallbackValue=88}" />
                        <TextBlock Style="{StaticResource ValueText}" Text="SBS : " />
                        <TextBlock Text="{Binding Aircraft.NumMessagesSBS, FallbackValue=120}" />
                    </StackPanel>

                    <TextBlock
                        Grid.Row="10"
                        Grid.ColumnSpan="2"
                        FontWeight="Bold"
                        Style="{StaticResource TitleText}"
                        Text="LAST UPDATE TIME:    " />
                    <TextBlock
                        Grid.Row="11"
                        Style="{StaticResource ValueText}"
                        Text="21:24:16:709" />
                </Grid>
            </GroupBox>

            <!--  ─── 3) Display Map / Purge ───  -->
            <GroupBox Margin="0,10,0,0" Header="">
                <StackPanel Orientation="Vertical">
                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                        <CheckBox
                            VerticalAlignment="Center"
                            Content="Display Map:"
                            FontWeight="Bold"
                            IsChecked="{Binding DisplayMapEnabled}" />
                    </StackPanel>
                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                        <CheckBox
                            Margin="0,4,0,0"
                            Content="Purge Stale"
                            IsChecked="{Binding ControlSettings.PurgeStale}" />
                        <TextBox
                            Width="40"
                            Margin="4,0,0,0"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            Text="{Binding ControlSettings.PurgeDuration}" />
                        <Button
                            Margin="8,0,0,0"
                            Padding="8,2"
                            Command="{Binding Cmd_Purge}"
                            Content="Purge Force" />
                    </StackPanel>
                </StackPanel>

            </GroupBox>

            <GroupBox Margin="0,10,0,0" Header="Raw Connection">
                <StackPanel Margin="8" Orientation="Vertical">
                    <!--  Connection Status + Address  -->
                    <DockPanel Margin="0,0,0,8" LastChildFill="True">
                        <!--  Signal Light  -->
                        <usercontrols:ConnectionSignal
                            Width="16"
                            Height="16"
                            ConnectionStatus="{Binding RawConnectStatus}" />

                        <TextBox
                            Margin="8,0,0,0"
                            Padding="8,0"
                            HorizontalContentAlignment="Left"
                            VerticalContentAlignment="Center"
                            Text="{Binding ControlSettings.RawAddress, FallbackValue=127.0.0.1}" />
                    </DockPanel>

                    <!--  SBS Host Input  -->
                    <StackPanel Orientation="Horizontal">
                        <!--  Connect Button  -->
                        <Button
                            Width="100"
                            Margin="24,0,8,0"
                            Command="{Binding Cmd_RawConnect}"
                            Content="🔗 Connect" />

                        <!--  Disconnect Button  -->
                        <Button
                            Width="100"
                            Command="{Binding Cmd_RawDisconnect}"
                            Content="❌ Disconnect" />
                    </StackPanel>

                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                        <GroupBox
                            Margin="10,10,0,0"
                            Padding="4,2"
                            Background="Transparent"
                            Header="Record"
                            Style="{x:Null}">
                            <StackPanel Orientation="Horizontal">
                                <!--  Record  -->
                                <Button
                                    Margin="0,0,8,0"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Command="{Binding Cmd_RawRecord}"
                                    Cursor="Hand">
                                    <StackPanel HorizontalAlignment="Center" Orientation="Vertical">
                                        <iconPacks:PackIconMaterial
                                            Width="24"
                                            Height="24"
                                            Foreground="#555555"
                                            Kind="Record" />
                                    </StackPanel>
                                </Button>

                                <!--  Stop  -->
                                <Button
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Command="{Binding Cmd_RawRecordStop}"
                                    Cursor="Hand">
                                    <StackPanel HorizontalAlignment="Center" Orientation="Vertical">
                                        <iconPacks:PackIconMaterial
                                            Width="24"
                                            Height="24"
                                            Foreground="#555555"
                                            Kind="Stop" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </GroupBox>
                        <GroupBox
                            Margin="10,10,0,0"
                            Padding="4,2"
                            Background="Transparent"
                            Header="Play"
                            Style="{x:Null}">
                            <StackPanel Orientation="Horizontal">
                                <!--  Play  -->
                                <Button
                                    Margin="0,0,8,0"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Command="{Binding Cmd_RawPlay}"
                                    Cursor="Hand">
                                    <StackPanel HorizontalAlignment="Center" Orientation="Vertical">
                                        <iconPacks:PackIconMaterial
                                            Width="24"
                                            Height="24"
                                            Foreground="#555555"
                                            Kind="Play" />
                                    </StackPanel>
                                </Button>

                                <!--  Stop  -->
                                <Button
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Command="{Binding Cmd_RawPlayStop}"
                                    Cursor="Hand">
                                    <StackPanel HorizontalAlignment="Center" Orientation="Vertical">
                                        <iconPacks:PackIconMaterial
                                            Width="24"
                                            Height="24"
                                            Foreground="#555555"
                                            Kind="Stop" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!--  ─── 7) SBS Record / SBS Playback ───  -->
            <GroupBox Margin="0,10,0,0" Header="SBS Connection">
                <StackPanel Margin="8" Orientation="Vertical">

                    <!--  Connection Status + Buttons  -->
                    <DockPanel Margin="0,0,0,8" LastChildFill="True">
                        <!--  Signal Light  -->
                        <usercontrols:ConnectionSignal
                            Width="16"
                            Height="16"
                            ConnectionStatus="{Binding SbsConnectStatus}" />
                        <TextBox
                            Margin="8,0,0,0"
                            Padding="8,0"
                            HorizontalContentAlignment="Left"
                            VerticalContentAlignment="Center"
                            IsEnabled="{Binding}"
                            Text="{Binding ControlSettings.SbsAddress}" />
                    </DockPanel>

                    <!--  SBS Host Input  -->
                    <StackPanel Orientation="Horizontal">
                        <!--  Connect Button  -->
                        <Button
                            Width="100"
                            Margin="24,0,8,0"
                            Command="{Binding Cmd_SbsConnect}"
                            Content="🔗 Connect" />

                        <!--  Disconnect Button  -->
                        <Button
                            Width="100"
                            Command="{Binding Cmd_SbsDisconnect}"
                            Content="❌ Disconnect" />
                    </StackPanel>

                    <!--  Icon Buttons  -->
                    <StackPanel
                        Margin="0,10,0,0"
                        HorizontalAlignment="Right"
                        Orientation="Vertical">
                        <CheckBox
                            Margin="10,0,0,0"
                            Content="Use BigQuery"
                            IsChecked="{Binding ControlSettings.UseBigQuery}" />

                        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                            <GroupBox
                                Margin="10,0,0,0"
                                Padding="4,2"
                                Background="Transparent"
                                Header="Record"
                                Style="{x:Null}">
                                <StackPanel Orientation="Horizontal">
                                    <!--  Record  -->
                                    <Button
                                        Margin="0,0,8,0"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        Command="{Binding Cmd_SbsRecord}"
                                        Cursor="Hand">
                                        <StackPanel HorizontalAlignment="Center" Orientation="Vertical">
                                            <iconPacks:PackIconMaterial
                                                Width="24"
                                                Height="24"
                                                Foreground="#555555"
                                                Kind="Record" />
                                        </StackPanel>
                                    </Button>

                                    <!--  Stop  -->
                                    <Button
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        Command="{Binding Cmd_SbsRecordStop}"
                                        Cursor="Hand">
                                        <StackPanel HorizontalAlignment="Center" Orientation="Vertical">
                                            <iconPacks:PackIconMaterial
                                                Width="24"
                                                Height="24"
                                                Foreground="#555555"
                                                Kind="Stop" />
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </GroupBox>
                            <GroupBox
                                Margin="10,0,0,0"
                                Padding="4,2"
                                Background="Transparent"
                                Header="Play"
                                Style="{x:Null}">
                                <StackPanel Orientation="Horizontal">
                                    <!--  Play  -->
                                    <Button
                                        Margin="0,0,8,0"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        Command="{Binding Cmd_SbsPlay}"
                                        Cursor="Hand">
                                        <StackPanel HorizontalAlignment="Center" Orientation="Vertical">
                                            <iconPacks:PackIconMaterial
                                                Width="24"
                                                Height="24"
                                                Foreground="#555555"
                                                Kind="Play" />
                                        </StackPanel>
                                    </Button>

                                    <!--  Stop  -->
                                    <Button
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        Command="{Binding Cmd_SbsPlayStop}"
                                        Cursor="Hand">
                                        <StackPanel HorizontalAlignment="Center" Orientation="Vertical">
                                            <iconPacks:PackIconMaterial
                                                Width="24"
                                                Height="24"
                                                Foreground="#555555"
                                                Kind="Stop" />
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                    </StackPanel>

                </StackPanel>
            </GroupBox>


            <!--  ─── 8) Areas Of Interest ───  -->
            <GroupBox Margin="0,10,0,0" Header="Areas Of Interest">
                <StackPanel Orientation="Vertical">
                    <ListView
                        x:Name="AreaListView"
                        Height="100"
                        BorderBrush="Gray"
                        BorderThickness="1"
                        ItemsSource="{Binding AreaList}"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        SelectedItem="{Binding SelectedArea, Mode=TwoWay}"
                        SelectionMode="Single">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn
                                    Width="160"
                                    DisplayMemberBinding="{Binding AreaName}"
                                    Header="Area" />
                                <GridViewColumn Width="80" Header="Color">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Rectangle
                                                Width="20"
                                                Height="16"
                                                Fill="{Binding AreaColorBrush}"
                                                Stroke="Black"
                                                StrokeThickness="0.5" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <!--  Insert / Complete / Cancel / Delete 버튼  -->
                    <StackPanel
                        Margin="0,4,0,0"
                        HorizontalAlignment="Left"
                        Orientation="Horizontal">
                        <Button
                            Width="60"
                            Command="{Binding InsertCommand}"
                            Content="Insert" />
                        <Button
                            Width="70"
                            Margin="4,0,0,0"
                            Command="{Binding CompleteCommand}"
                            Content="Complete" />
                        <Button
                            Width="60"
                            Margin="4,0,0,0"
                            Command="{Binding CancelCommand}"
                            Content="Cancel" />
                        <Button
                            Width="60"
                            Margin="4,0,0,0"
                            Command="{Binding DeleteCommand}"
                            Content="Delete" />
                    </StackPanel>
                </StackPanel>

            </GroupBox>


            <!--  ─── 9) 지도 공급자 선택 콤보박스 ───  -->
            <GroupBox Margin="0,10,0,0" Header="ETC">
                <StackPanel Orientation="Vertical">
                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                        <TextBlock FontWeight="Bold" Text="Map provider :" />
                        <ComboBox
                            Width="150"
                            Margin="8,0,0,0"
                            ItemsSource="{Binding TileServerTypeList}"
                            SelectedItem="{Binding SelectedTileServer}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontWeight="Bold" Text="CPA TIME:" />
                        <TextBlock Margin="8,0,0,0" Text="NONE" />
                    </StackPanel>
                    <StackPanel Margin="0,2,0,0" Orientation="Horizontal">
                        <TextBlock FontWeight="Bold" Text="CPA DISTANCE:" />
                        <TextBlock Margin="8,0,0,0" Text="None" />
                    </StackPanel>

                    <DockPanel Margin="0,8,0,0" VerticalAlignment="Center">
                        <CheckBox
                            Content="Time-To-Go"
                            FontWeight="Bold"
                            IsChecked="{Binding UseTimeTogo}" />
                        <TextBox
                            Width="100"
                            Margin="10,0,0,0"
                            HorizontalAlignment="Left"
                            HorizontalContentAlignment="Center"
                            Text="{Binding TimeTogoValue, Mode=TwoWay, Converter={StaticResource secToTime}}" />
                        <!--<TextBlock Margin="8,0,0,0" Text="{Binding ElementName=sliderTimeTogo, Path=Value, Converter={StaticResource secToTime}, StringFormat='hh\\:mm\\:ss'}" />-->
                    </DockPanel>

                    <!--  Time-To-Go 슬라이더 + 값 표시  -->
                    <Slider
                        x:Name="sliderTimeTogo"
                        Margin="0,2,20,0"
                        LargeChange="10"
                        Maximum="1800"
                        Minimum="0"
                        SmallChange="1"
                        TickFrequency="1"
                        Value="{Binding TimeTogoValue}" />

                    <!--  Zoom In / Zoom Out 버튼  -->
                    <StackPanel
                        Margin="0,8,0,8"
                        HorizontalAlignment="Center"
                        Orientation="Horizontal">
                        <Button Width="100" Content="Zoom In" />
                        <Button
                            Width="100"
                            Margin="8,0,0,0"
                            Content="Zoom Out" />
                    </StackPanel>
                </StackPanel>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>
